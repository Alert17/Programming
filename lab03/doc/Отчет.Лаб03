Закусілов Микита. КІТ - 120а.

1.Для вирішення завдань зроблено нову директорію в яку додали каталог "src" де зберігається головний файл "main.c" де знаходяться всі завдання.
2.Додано Makefile, в якому прибрав "-Werror" і всі операції над тестовими файлами, яких у мене в роботі немає.
3.Задача за варіантом - №4. Де нам потрібно переводити чотирьохрозрядне чисто з десяткової системи числення в будь-яку задану.
4. Ми оголошуємо змінні, нам потрібна ступінь системи обчислення - "p", змінну цілого числа, яке ми будемо переводити - number4, 4 змінних, в які ми будемо вносити цифру кожного розряду, для перекладу між система обчислення, і остання змінна - результ (result), в якому ми отримаємо число в заданої системі.
5. Для початку нам потрібно повне число розбити на розряди, для цього ми використовуємо  оператори ділення (/) і залишок від ділення (%).
6. Після того як ми знайшли кожен розряд нам потрібно зібрати число вже в потрібному комп'ютері. Кожне число - це розряд, починаючи з кінця ми множимо число, яке у нас є на ступінь "р", а після додаючи кожне наступне число, помножене на ступінь n + 1 (num44 + num34 * p + num24 * p * p + num14 * p * p * p).
